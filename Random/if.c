#include <stdio.h>

int main() {

	if  (2 < 5)
		printf("2 is less than 5.\n");

	if (2 < 5) {
		printf("I'll always print this like.\n");
		printf("because 2 is always less than 5!\n");
	return 0;
	}
}
